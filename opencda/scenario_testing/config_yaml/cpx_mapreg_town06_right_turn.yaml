base_config: cpx_single_right_lane_turn.yaml

description: Map-layer regression for a right lane change and junction connector.

vehicle_base:
  planner:
    global_planner_mode: dij
    global_planner_strict: true
    global_planner_xodr_path: Global_Planner/maps/Town06.xodr

scenario:
  single_cav_list:
    - name: cav1
      spawn_position: [225.10, -20.1, 0.3, 0, 179.8, 0]
      destination: [6.3, -46.9, 0.3]
      destination_lane_extension_m: 50.0
      v2x:
        communication_range: 60
      planner:
        debug_output_dir: opencda/planning_module/opencda_bridge/mapreg_town06_right_turn
        target_speed_mps: 5.0
