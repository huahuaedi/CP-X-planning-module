1. mpc replan if fails for 4 consecutive times.-- Done
2. only consider the leading vehicle for the ego lane for lane safety score, for other lanes use the previous rules. -- Done
3. use road boundary instead of lane boundary.
4. use cp messages instead of finding the intersection and signal state manually.
5. in the intersection mode, if dist(ego,intersection)>threshold: lane follow, else: reroute.
6. check if obstacle collision potential is being implemented for each mpc point



vru x = 2700


1. town6_scenario_1: RLW, hazard
2. town6_scenario2: route change, vru
3. nav thorugh difficult geometric condition



you will work inside the following directory "/home/umd-user/Desktop/OpenCDA/opencda/planning_module". Now you need to create some scenario in the planning_module/opencda_scenario. check the previous scenarios in that folder to see how the previous scenarios were created. the traffic should be generated by sumo but some specific traffic or obstacles should be generated according to the requirements (see how the other scenarios are created). you will use the following map to create the scenarios "/home/umd-user/carla_source/carla/Unreal/CarlaUE4/Content/Carla/Maps/Town06_Opt.umap". each scenario should be independent and should contain the scenairo specific logic in that scenario folder.  create necessary files and folders . here is the first scenario

rules: 
1. find all the vehicle markers (object : cube), named as "vehicle_N" where N = 1,2,3,.... and spawn vehicles there. 
if N is even and N<=50, auto pilot will be off for those vehicles, if N is odd  autopilot will be on at the beginning 
for those vehicles after spawn. However, if N > 50 and even then at the beginning after spawn autopilot will be off 
but when the ego reaches a certain distance (trigger distance, default : 20m (tunable in the yaml file)), auto pilot will be on for those vehicles.

2. find all the markers like the preivous one named "vru_N".  spawn a person at the marker for N = odd. and they will be static. when the ego reaches the trigger distance (tunable) . the person will be movimg towards the position of the immediate even number marker (e.g, person spawned at position vru_1 will start moving towards the position of the marker vru_2, similarly vru_3 will start moving towards vru_4). see all_usecase_scenario folder to check how the vehicles and vru markers are found and how the vrus movements are handled.

3. also traffic should be generated using sumo.
4. find all the markers named "stop_N" and "intersection_N" . when the distance between the ego and this markers are less then the trigger distance, register a cp_message at the [cp_message.json](opencda/planning_module/behavior_planner/cp_message.json) file. check the message format from all_usecase_scenario
the above are the general rules for all scenario. keep them in mind. 


scenario name: "town6_scenario_1"
1. spawn ego at the marker position ("town6_scenario_1_ego") and the final destination should be at the marker position named "town6_scenario_1_final_destination"



note: 
1. latest checkpoint
2. ref waypoint building fixed.


Town10:
scenario1: rlvw
scenario2: hazard
scenario3: vru
scenario4: foreign object
scenario5: high level planning
scenario6: difficult geometric condition


issues:
1. lane safetly score calculation need to be fixed.
2. use the waypoints between ego and the blue dot as lane center ref
3. when the blue dot snaps to the intersection, if the optimal lane is free, it should snap there.